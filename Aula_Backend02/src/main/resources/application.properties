# Configurações do banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/paloma_eventos?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuração JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.show-sql=true               
spring.jpa.properties.hibernate.format_sql=true

# Configuração do console do H2 (opcional, caso você queira usar o H2 para testes em memória)
# spring.h2.console.enabled=true

# Configuração de log (opcional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
logging.level.org.springframework.security=DEBUG
logging.level.com.example=DEBUG


# Perfil de ambiente (opcional, você pode usar profiles como "dev", "prod", etc.)
# spring.profiles.active=dev

# Configuração de servidor
server.port=8080
# Configuração de timezone
spring.datasource.timezone=UTC

jwt.secret=s3Cr3t!JWttOKEn-SegurA@2024#
jwt.expiration=3600000